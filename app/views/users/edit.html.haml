- provide(:title, 'Edit profile')
%h1 Edit profile: #{current_user.name}
.row
  .col-md-6.col-md-offset-3
    = simple_form_for(@user) do |f|
      = render 'form', f: f
      = f.submit "Save changes", class: "btn btn-default"
